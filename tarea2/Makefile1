CC = g++
CFLAGS = -std=c++11 $(shell pkg-config --cflags opencv4) -lpthread
LIBS = $(shell pkg-config --libs opencv4)

all: paralelo_thread

paralelo_thread: paralelo_thread.cpp
	$(CC) $(CFLAGS) -o paralelo_thread paralelo_thread.cpp $(LIBS)

clean:
	rm -f paralelo_thread
